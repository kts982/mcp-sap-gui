# MCP SAP GUI Server Requirements

# Core dependencies
mcp>=1.0.0              # Model Context Protocol SDK
pywin32>=306            # Windows COM automation (required for SAP GUI)

# Optional dependencies
pillow>=10.0.0          # For screenshot manipulation (optional)

# Development dependencies
pytest>=7.0.0           # Testing
pytest-asyncio>=0.21.0  # Async test support
mypy>=1.5.0             # Type checking
ruff>=0.1.0             # Linting
